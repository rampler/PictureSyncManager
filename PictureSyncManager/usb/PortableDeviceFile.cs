namespace PictureSyncManager
{
    public class PortableDeviceFile : PortableDeviceObject
    {
        public PortableDeviceFile(string id, string name, string date, string size) : base(id, name, date, size)
        {
        }
    }
}