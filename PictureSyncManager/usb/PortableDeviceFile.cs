namespace PictureSyncManager
{
    public class PortableDeviceFile : PortableDeviceObject
    {
        public PortableDeviceFile(string id, string name, string date) : base(id, name, date)
        {
        }
    }
}